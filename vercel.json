{
	"rewrites": [
		{
			"source": "/:path*",
			"destination": "/:path*.html"
		},
		{
			"source": "/:path*/",
			"destination": "/:path*/index.html"
		}
	],
	"buildCommand": "git lfs pull && npx quartz build",
	"outputDirectory": "public",
	"installCommand": "npm install && apt-get update && apt-get install -y git-lfs && git lfs install",
	"devCommand": "npx quartz build --watch --serve"
}